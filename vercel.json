{
  "framework": "nextjs",
  "installCommand": "npm install && cd web && npm install --legacy-peer-deps",
  "buildCommand": "cd web && npx prisma migrate deploy && npm run build",
  "outputDirectory": "web/.next"
}
